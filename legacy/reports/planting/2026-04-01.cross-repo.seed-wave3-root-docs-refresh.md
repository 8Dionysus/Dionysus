# Planting Report

## Metadata

- Date: 2026-04-01
- Wave: none (bounded cross-repo root docs refresh)
- Seed: `seed.wave3.root-docs-refresh.v0`
- Source ref: `archive/seed_pack_exports/seed_wave3_root_docs_refresh_2026-04-02.zip`
- Target repo: `cross-repo: aoa-techniques, aoa-skills, aoa-evals, aoa-agents, aoa-sdk, ATM10-Agent`
- Planting status: landed
- PR / commit / issue: `aoa-techniques` PR `#137` merged as `d21ed79`; `aoa-skills` PR `#112` merged as `b27ad07`; `aoa-evals` PR `#60` merged as `62ce773`; `aoa-agents` PR `#32` merged as `ad8b8ea`; `aoa-sdk` pushed to `main` as `36a25d8`; `ATM10-Agent` updated cloud-only and pushed to `main` as `31da2c1`; `Dionysus` pack staged as `5808b0a`
- Maintainer: Codex

## Contract being planted

This planting applies one bounded root-entry refresh across the remaining
third-wave AoA repositories without rewriting deeper doctrine. The root
`README.md` files are shortened and made more route-first, while the root
`AGENTS.md` files become clearer about boundaries, working order, and
validation. The owning repositories keep their meaning; `Dionysus` only
preserves the bundle, the transplant map, and the durable trace.

## Landed surfaces

- Human-readable surface: root `README.md` in `aoa-techniques`, `aoa-skills`, `aoa-evals`, `aoa-agents`, `aoa-sdk`, and `ATM10-Agent`
- Structural surface: root `AGENTS.md` in `aoa-techniques`, `aoa-skills`, `aoa-evals`, `aoa-agents`, `aoa-sdk`, and `ATM10-Agent`
- Validation surface: `python scripts/validate_seed_surfaces.py`, `python scripts/release_check.py`, `python scripts/validate_repo.py`, `python scripts/build_published_surfaces.py`, `python scripts/validate_agents.py`, `python -m pytest -q`, `python -m ruff check .`, and `aoa workspace inspect /srv/aoa-sdk`

## Structural artifact

`seed_staging/root_docs/seed_wave3_root_docs_refresh.map.yaml` is the structural artifact that keeps
the repo order, scope limits, guardrails, and source bundle replayable from
inside `Dionysus`.

## Validation

- Command run: `python scripts/validate_seed_surfaces.py`
- Result: passed
- Manual review notes: `Dionysus` stayed aligned after bundle intake, prep-pack linkage, report hydration, and root README visibility updates

- Command run: `python scripts/release_check.py`
- Result: passed
- Manual review notes: `aoa-techniques` needed one bounded same-pass repair in root `README.md` so existing repo-doc discoverability tests and generated repo-doc parity surfaces stayed valid

- Command run: `python scripts/release_check.py`
- Result: passed
- Manual review notes: `aoa-skills` root refresh stayed docs-only and its long validator lane remained green, including the additional `build-validate` check on the protected PR

- Command run: `python scripts/validate_repo.py`
- Result: passed
- Manual review notes: `aoa-evals` accepted the shorter root shape without requiring adjacent sync

- Command run: `python scripts/build_published_surfaces.py` and `python scripts/validate_agents.py`
- Result: passed
- Manual review notes: `aoa-agents` root refresh preserved published-surface and consumer-seam integrity

- Command run: `python -m pytest -q`, `python -m ruff check .`, and `aoa workspace inspect /srv/aoa-sdk`
- Result: passed
- Manual review notes: `aoa-sdk` root refresh preserved workspace topology and compatibility posture; the workspace inspection still resolved only the intended local federation roots

- Command run: `python -m pytest`
- Result: blocked by local environment
- Manual review notes: the temporary cloud-only `ATM10-Agent` working checkout failed during test collection because `numpy` was not installed locally; root markdown links were verified and the publish path remained cloud-only as requested

- Command run: local markdown-link resolver across the refreshed root `README.md` and `AGENTS.md` files
- Result: passed
- Manual review notes: all local markdown links in the refreshed roots resolve from their owning repository roots or cloud-only temporary checkout

## Deferred zones / non-goals

- no deep doc rewrites outside root `README.md` and root `AGENTS.md`
- no wave manifest changes
- no `seed_index` promotion
- no new public/private status claims
- no ownership transfer away from the existing repo homes

## Risks / follow-ups

- `aoa-skills` and `aoa-evals` intentionally lose a large amount of root-level inventory, so future detail growth should continue in linked docs and generated surfaces rather than reflating the root.
- `aoa-sdk` depends on workspace-topology docs and tests staying aligned as the SDK grows.
- `ATM10-Agent` is cloud-only for this wave; any temporary local checkout used to publish the refresh should not be mistaken for a new canonical workspace home.

## Vocabulary preserved

- route-first
- public practice canon
- bounded execution
- bounded proof
- role-bearing actor
- control plane
- dry-run by default
- boundary-explicit
