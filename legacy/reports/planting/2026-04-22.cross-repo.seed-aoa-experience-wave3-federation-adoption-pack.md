# Planting Report

## Metadata

- Date: 2026-04-22
- Wave: Experience Wave 3
- Seed: `seed.aoa.experience-wave3-federation-adoption-pack.v0`
- Source ref:
  - `/home/dionysus/Загрузки/aoa-experience-federation-harvest-seed-v0_6.zip`
  - `/home/dionysus/Загрузки/aoa-experience-adoption-forge-seed-v0_7.zip`
- Target repo: `cross-repo: Dionysus, Agents-of-Abyss, Tree-of-Sophia, aoa-evals, aoa-playbooks, aoa-stats, aoa-memo, aoa-routing, aoa-agents, aoa-sdk, aoa-kag, aoa-skills, aoa-techniques, abyss-stack`
- Planting status: landed in the local workspace; not committed or merged
- PR / commit / issue: none in this session yet
- Dionysus lifecycle cleanup: added this durable cross-repo planting trace for the v0.6 federation harvest and v0.7 adoption forge landing
- Maintainer: Codex with agent slices from Kant, Ptolemy, Plato, and Signal

## Contract Being Planted

Experience Wave 3 plants v0.6 federation harvest as the active center-owned
program for recognizing source-owned recurrence across repositories, then
plants v0.7 adoption forge as the owner-gated follow-up path. The wave makes
shared patterns answer to owner-local evidence, bounded evals, lineage checks,
reviewed KAG and ToS candidate boundaries, explicit owner consent, shadow
adoption, rollback or quarantine, and retention review.

The wave does not approve harvest by Codex, promote directly to KAG, write
directly to `Tree-of-Sophia`, self-rewrite assistants, force owner adoption,
activate runtime behavior, author routing meaning, or bypass rollback and
retention.

## Landed Surfaces

- Human-readable surface: `Agents-of-Abyss/docs/EXPERIENCE_WAVE3_FEDERATION_ADOPTION.md`, owner-local Wave 3 docs across `Tree-of-Sophia`, `aoa-agents`, `aoa-evals`, `aoa-playbooks`, `aoa-memo`, `aoa-stats`, `aoa-routing`, `aoa-sdk`, `aoa-kag`, `aoa-skills`, `aoa-techniques`, and `/home/dionysus/src/abyss-stack`
- Structural surface: `Agents-of-Abyss/schemas/experience-wave3-federation-adoption.schema.json`, `Agents-of-Abyss/examples/experience_wave3_federation_adoption.example.json`, and strict owner-local Wave 3 schema/example pairs across the owner repos
- Validation surface: `Agents-of-Abyss/scripts/validate_experience_wave3.py`, central Wave 3 tests, and generated owner-local contract tests that validate examples and reject unknown top-level, `refs`, and `payload` fields

## Structural Artifact

`Agents-of-Abyss/examples/experience_wave3_federation_adoption.example.json`
is the center structural anchor. It preserves the ordered v0.6 federation
spine, the ordered v0.7 adoption spine, the explicit Codex stop-lines, the
human authority requirements, and the owner split that prevents the center from
absorbing proof, memory, routing, KAG, runtime, or ToS authority.

## Validation

- Command run: `unzip -t` for both supplied v0.6-v0.7 archives
- Result: passed
- Manual review notes: transport archives were readable before import.

- Command run: `python scripts/validate_experience_wave3.py` and `python -m pytest -q tests/test_experience_wave3.py`
- Result: passed in `Agents-of-Abyss`, targeted tests `9 passed`
- Manual review notes: the center validator rejects Codex KAG promotion, direct ToS write authority loss, missing owner consent, missing rollback/quarantine, missing retention, and routing meaning theft.

- Command run: `python -m pytest -q tests/test_experience_wave3_seed_contracts.py`
- Result: passed in `Tree-of-Sophia`, `aoa-evals`, `aoa-playbooks`, `aoa-stats`, `aoa-memo`, `aoa-routing`, `aoa-agents`, `aoa-sdk`, `aoa-kag`, `aoa-skills`, `aoa-techniques`, and `/home/dionysus/src/abyss-stack`, targeted tests `2 passed` in each repo
- Manual review notes: owner-local Wave 3 schemas are strict against unknown top-level, `refs`, and `payload` fields and reject wrong payload types.

- Command run: `python scripts/release_check.py`
- Result: passed in every changed source repo after whitespace cleanup and staged sanitization review
- Manual review notes: full release checks passed for `Agents-of-Abyss` (`121 passed`), `Dionysus` (`65 passed`), `Tree-of-Sophia` (`50 unittest`), `aoa-evals` (`229 passed`), `aoa-playbooks` (`90 passed`), `aoa-stats` (`98 passed`), `aoa-memo` (`172 passed`), `aoa-routing` (`217 passed`), `aoa-agents` (`93 unittest`), `aoa-sdk` (`366 passed`, Ruff, mypy, package build), `aoa-kag` (`97 unittest` plus KAG validation), `aoa-skills` (`355 unittest` plus skill validators), `aoa-techniques` (`116 unittest` plus repo validation), and `/home/dionysus/src/abyss-stack` (`211 passed` with 3 pre-existing warnings).

- Command run: `scripts/aoa-sync-configs`
- Result: passed after an `rsync -ani` dry-run; no `--delete`; no service start
- Manual review notes: sync was needed only because the deployed `/srv/abyss-stack/Configs` mirror already existed and was missing source-managed docs/scripts from recent source commits. The follow-up `abyss-stack` release check passed source/deployed parity.

- Command run: staged `git diff --cached --check` and staged secret-pattern scan
- Result: passed
- Manual review notes: public-share guard surfaced sanitization review; staged content had no secret-pattern findings after whitespace cleanup.

- Command run: required-check audit helper
- Result: skipped
- Manual review notes: this session has not touched GitHub workflow names, required-check contracts, main-branch protection, or branch-protection settings.

## Deferred Zones / Non-goals

- no new `aoa-experience` repository
- no hidden runtime activation
- no Codex harvest approval, owner adoption approval, KAG promotion, or ToS intake approval
- no assistant self-rewrite or hidden adoption
- no direct `Tree-of-Sophia` write from federation harvest, KAG, runtime, or assistant surfaces
- no stats-as-proof, memo-as-verdict, routing-as-owner-truth, SDK-local adoption authority, or Dionysus-owned adoption authority
- no live services, host ports, scheduler loops, runtime workers, or deployment daemon
- no import of `.pytest_cache/`, `__pycache__/`, generated seed transport output, or archive-local cache noise

## Risks / Follow-ups

- The Wave 3 landing is local workspace state, not merged upstream. Future
  sessions should treat live owner repos as the first reading surface and this
  report as trace.
- KAG and ToS promotion remain candidate/dossier routes only until their owner
  review surfaces accept them.
- Owner adoption remains a second sovereign act after federation harvest; a
  shared pattern is never enough by itself.
- `aoa-routing` owns route signals only and must keep gate, dossier, and
  adoption wording advisory.
- `abyss-stack` source truth is `/home/dionysus/src/abyss-stack`; the deployed
  `/srv/abyss-stack` tree is runtime/deployed posture, not the git owner
  checkout.

## Vocabulary Preserved

- owner-local signals
- cross-repo recurrence
- federation pattern candidate
- harvest gate
- shared pattern registry
- KAG promotion dossier
- Tree-of-Sophia candidate dossier boundary
- owner-local adoption quest
- owner consent
- compatibility matrix
- shadow adoption
- adoption decision
- rollback or quarantine
- retention check
