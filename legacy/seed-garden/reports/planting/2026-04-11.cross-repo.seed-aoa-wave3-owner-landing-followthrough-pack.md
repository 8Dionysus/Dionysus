# Planting Report

## Metadata

- Date: 2026-04-11
- Wave: none (Wave 3 owner-followthrough closeout)
- Seed: `seed.aoa.wave3-owner-landing-followthrough-pack.v0`
- Source ref: `archive/seed_pack_exports/aoa-wave3-owner-landing-followthrough-pack-2026-04-11.zip`
- Target repo: `cross-repo: Agents-of-Abyss, aoa-sdk, aoa-skills, Dionysus, aoa-stats, aoa-playbooks, aoa-memo`
- Planting status: landed
- PR / commit / issue:
  - `Agents-of-Abyss` PR #58 merged as `bf18a78304e53cb15406bddab62a50b6b9e67a3e`
  - `aoa-sdk` PR #56 merged as `6e8ddb705202d84cc61c5f6da94a4ef9f653bc2f`
  - `aoa-skills` PR #162 merged as `15bca21d711318fd21bed727c1a2eec6f82c688d`
  - `Dionysus` PR #75 merged as `63dab45d2e5ac807d830b4e46606190e5e2b3529`
  - `aoa-stats` PR #23 merged as `8b494e7c1c08a8f1ee84a3d68e240dc420af1585`
  - `aoa-playbooks` PR #92 merged as `1724824619331a1aad4968e379cba9e9d6e9103b`
  - `aoa-memo` PR #74 merged as `e760dbf2ff27ffdb6ccd10e03fac2e3e4932efc2`
- Dionysus lifecycle cleanup: updated the staged wave3 prep-pack marker to `landed_upstream_retained_for_lineage`, aligned `docs/SEED_SURFACE_MAP.md`, and preserved this report as the merged wave witness
- Maintainer: Codex

## Contract being planted

This closeout records that the Wave 3 owner-followthrough packet is no longer a
future queue in `Dionysus`. Its owner slices now live in the owner repos, while
`Dionysus` keeps only the packet, lifecycle marker, and planting trail.

Wave 3 stays subordinate to the first-wave lineage axis
`cluster_ref -> candidate_ref -> seed_ref -> object_ref` and to the staged
wave2 downstream convergence route. It hardens what happens after a reviewed
`candidate_ref` already exists: tracked owner-status landing, bounded
follow-through, seed-to-owner trace, visible pruning, and bounded memo
writeback without letting any downstream layer claim owner truth.

The landed split remains strict:

- `Agents-of-Abyss` owns owner-landing and pruning doctrine
- `aoa-sdk` owns reviewed-closeout follow-through carry
- `aoa-skills` owns reviewed owner-status and governed follow-through surfaces
- `Dionysus` owns seed-to-owner landing trace
- `aoa-stats` owns derived owner-landing and supersession/drop summaries
- `aoa-playbooks` owns the narrower owner-followthrough campaign
- `aoa-memo` owns bounded prune-aware writeback

## Landed surfaces

- Human-readable surface: `docs/OWNER_LANDING_AND_PRUNING.md` in `Agents-of-Abyss`; `docs/CLOSEOUT_FOLLOWTHROUGH_MAP.md` in `aoa-sdk`; `docs/OWNER_STATUS_SURFACES.md` in `aoa-skills`; `playbooks/owner-followthrough-campaign/PLAYBOOK.md` in `aoa-playbooks`; `docs/GROWTH_REFINERY_WRITEBACK.md` in `aoa-memo`
- Structural surface: `schemas/closeout_owner_followthrough_map.schema.json` in `aoa-sdk`; `schemas/reviewed_owner_landing_bundle.schema.json` and `schemas/route_followthrough_decision.schema.json` in `aoa-skills`; `schemas/seed_owner_landing_trace.schema.json` in `Dionysus`; `schemas/owner_landing_summary.schema.json` and `schemas/supersession_drop_summary.schema.json` in `aoa-stats`
- Validation surface: `python scripts/validate_workspace_control_plane.py` in `aoa-sdk`; `python scripts/validate_skills.py --fail-on-review-truth-sync` in `aoa-skills`; `python scripts/validate_seed_surfaces.py` in `Dionysus`; `python scripts/validate_repo.py` in `aoa-stats`; `python scripts/validate_playbooks.py` in `aoa-playbooks`; `python scripts/validate_memo.py` in `aoa-memo`

## Structural artifact

`Dionysus/examples/seed_owner_landing_trace.example.json` is the structural
artifact because it keeps seed staging honest after reviewed candidate identity
already exists while still refusing to upgrade the seed garden into final owner
authority.

## Validation

- Command run: `cd /srv/Dionysus && python scripts/validate_seed_surfaces.py`
- Result: passed
- Manual review notes: the wave3 seed note, lifecycle validator expectation, and this planting report agree on the landed-upstream posture.

- Command run: `cd /srv/Dionysus && python scripts/build_seed_route_map.py --check`
- Result: passed
- Manual review notes: the compact route map stayed aligned after the lifecycle closeout.

- Command run: `cd /srv/Dionysus && python scripts/validate_seed_route_map.py`
- Result: passed
- Manual review notes: the route map still points to the archived packet and retains the right owner split.

- Command run: `cd /srv/Dionysus && python -m pytest -q tests`
- Result: passed
- Manual review notes: seed lifecycle and route-map expectations stayed green after the closeout changes.

## Deferred zones / non-goals

- no replay of already-landed owner slices into duplicate repo surfaces
- no claim that `Dionysus` became the owner of landing, follow-through, stats, playbook, or memo truth
- no widening into wave2 downstream convergence work from this closeout
- no Wave D automation

## Risks / follow-ups

- Treat this pack as lineage-only replay evidence from here on out. Any later
  owner-followthrough work should start from the live owner repos, not from the
  archived bundle tree.
- Keep `status_posture` grammar pinned to the first-wave vocabulary and keep
  terminal outcomes in lifecycle or outcome fields rather than widening posture.
- When wave2 eventually closes, revisit only the route ordering between the two
  packs, not the already-landed owner boundaries recorded here.

## Vocabulary preserved

- `cluster_ref`
- `candidate_ref`
- `seed_ref`
- `object_ref`
- owner-status landing
- governed follow-through
- seed-to-owner landing trace
- supersession/drop summary
- prune-aware writeback
