# Targeted Repo-Homes Saturation Pass

Date: 2026-03-24
Status: landed audit sync

## Purpose

This note records the targeted repo-homes saturation pass for the two remaining
AoA seeds that still looked materially under-landed outside their primary
homes.

It does not change lifecycle states.
It records that the missing repo-owned landing slices now exist in
`aoa-techniques` and `aoa-skills`, then classifies the remaining ToS-facing
items that were only waiting on stricter bookkeeping.

## Closed by explicit repo-owned landing slices

### `aoa-ontology-spine`

Closed for the current targeted saturation pass through:

- `aoa-techniques/docs/ECOSYSTEM_CONTEXT.md`
- `aoa-skills/docs/LAYER_POSITION.md`

Those surfaces now make the layer map explicit from both sides:

- techniques as the reusable-practice layer
- skills as one bounded execution layer

This removes the remaining repo-home ambiguity that had persisted outside the
primary architecture homes.

### `aoa-method-lives-in-playbooks`

Closed for the current targeted saturation pass through:

- `aoa-techniques/docs/ECOSYSTEM_CONTEXT.md`
- `aoa-skills/docs/LAYER_POSITION.md`

Those surfaces now make the boundary explicit on both sides:

- `aoa-techniques` does not absorb scenario method
- `aoa-skills` packages workflows but does not become the home of recurring
  scenario canon
- `aoa-playbooks` remains the explicit home of method

## Support-complete in current public homes

### `tos-practice-branch-of-the-tree`

This seed is support-complete in its current public homes.

- `Tree-of-Sophia` remains the primary knowledge home
- AoA-side implementation support already exists where it needs to exist
- any later AoA or `aoa-kag` saturation stays optional derived work

### `tos-human-curated-ai-amplified-expansion`

This seed is support-complete in `Tree-of-Sophia`.

- the primary human-curation posture already lives in the ToS surface
- AoA-side operational reinforcement remains optional later work
- there is no present blocker at the repo-home level

## Non-goals

- do not reopen `aoa-concept-operation-counterpart-edges`
- do not promote `AOA-K-0007` or `AOA-K-0008`
- do not touch `wider-world-thought-expansion`
- do not open a full listed-`repo_homes` saturation campaign
