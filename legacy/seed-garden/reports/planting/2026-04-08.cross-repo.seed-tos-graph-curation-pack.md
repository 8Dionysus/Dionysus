# Planting Report

## Metadata

- Date: 2026-04-08
- Wave: none (future prep pack closeout)
- Seed: `seed.tos.graph-curation-pack.v0`
- Source ref: `archive/seed_pack_exports/tos_graph_curation_seed.zip`
- Target repo: `cross-repo: Dionysus, abyss-stack`
- Planting status: partially landed
- PR / commit / issue:
  - `Dionysus` stage commit `8c181fc`
  - `abyss-stack` owner-rollout commits `c5c5528`, `86350a0`, `a4cec1f`
- Dionysus lifecycle cleanup: updated linked prep-pack markers from staged-only to partially landed lineage
- Maintainer: Codex

## Contract being planted

This planting turns the ToS graph curation zip from staged-only transport into
an honest lineage pack after a verified owner rollout in `abyss-stack`.
The landed slice keeps `Tree-of-Sophia` canonical, keeps Neo4j projection-only,
and keeps the helper localhost-only while making route-first inspection and
route-scoped projection sync real.

The pack is only partially landed on purpose.
It now covers the first owner-local read/projection slice plus Dionysus
closeout, while any later writeback lane and any repo-local `Tree-of-Sophia`
hardening remain deferred behind validator law.

## Landed surfaces

- Human-readable surface: `docs/TOS_GRAPH_CURATION.md` in `abyss-stack`
- Structural surface: `config-templates/Services/tos-graph/app/neo4j_store.py` in `abyss-stack`
- Validation surface: `python scripts/validate_seed_surfaces.py`, `python -m pytest -q tests`, `python scripts/validate_stack.py`, and `scripts/aoa-smoke --profile curation`

## Structural artifact

`config-templates/Services/tos-graph/app/neo4j_store.py` is the structural
artifact because it makes the route-scoped projection contract inspectable:
`Tree-of-Sophia` stays read-only, route-local projection state can be replaced
cleanly, and Neo4j remains a derived surface rather than canonical truth.

## Validation

- Command run: `cd /srv/Dionysus && python scripts/validate_seed_surfaces.py`
- Result: passed
- Manual review notes: checked the prep-pack note, lifecycle buckets in `docs/SEED_SURFACE_MAP.md`, the new planting report, and the quest closeout against the already-landed owner rollout

- Command run: `cd /srv/Dionysus && python -m pytest -q tests`
- Result: passed
- Manual review notes: the closeout stayed inside seed-garden lineage and quest surfaces without reopening wave order or seed-index control

- Command run: `cd /home/dionysus/src/abyss-stack && python scripts/validate_stack.py`
- Result: passed
- Manual review notes: verified against the source checkout, not `/srv/abyss-stack`, after the route-sync landing

- Command run: `cd /home/dionysus/src/abyss-stack && scripts/aoa-smoke --profile curation`
- Result: passed
- Manual review notes: the bounded runtime slice stayed localhost-only and `tos-graph` health stayed green after the route-sync rollout

## Deferred zones / non-goals

- no canonical writeback into `Tree-of-Sophia` yet
- no `Tree-of-Sophia` repo-local patch/apply surfaces from this pass
- no seed-index promotion or new numbered wave
- no claim that Neo4j is canonical truth
- no widening of host exposure beyond localhost

## Risks / follow-ups

- The pack is now partially landed, so any later mutation lane should start
  from live `abyss-stack` truth and current `Tree-of-Sophia` validators rather
  than from the zip bundle itself.
- If the later writeback lane requires clearer ToS-side repo contracts, land
  them as a bounded `Tree-of-Sophia` slice instead of silently growing more
  authority inside `abyss-stack` or `Dionysus`.
- If `tos-graph` grows beyond route-scoped projection and curation help, pause
  and recheck whether runtime convenience is starting to impersonate source
  ownership.

## Vocabulary preserved

- route-first
- projection-only
- validator boundary
- canonical ToS authority
- lineage
- transport bundle
- writeback deferred
