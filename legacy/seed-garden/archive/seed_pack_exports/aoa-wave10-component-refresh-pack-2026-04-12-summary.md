# AoA Wave 10 Component Refresh Pack Summary

- Archived bundle: `archive/seed_pack_exports/aoa-wave10-component-refresh-pack-2026-04-12.zip`
- Archived at: `2026-04-12`
- Source upload name: `aoa_wave10_component_refresh_pack.zip`
- Working role: staged future prep pack for component refresh and self-maintenance triggers

## Bundle shape

The bundle carries one bounded Wave 10 route across:

- `Agents-of-Abyss` for the center doctrine that names component refresh law
- `8Dionysus` for the shared workspace route and the first shared-root refresh law
- `aoa-sdk` for drift-hint carry and reviewed follow-through decisions
- `aoa-skills` for the skills-export refresh law
- `aoa-agents` for the Codex subagent projection refresh law
- `aoa-stats` for the derived component-refresh summary family
- `aoa-playbooks` for one recurring component-refresh coordination route
- `aoa-memo` for bounded refresh recovery-pattern writeback

## Preserved boundary

Keep the route owner-first:

- owner repos name refresh law, proof commands, rollback anchors, and refresh receipts
- `aoa-sdk` carries hints and reviewed follow-through only; it does not auto-mutate
- `aoa-stats` summarizes refresh status; it does not overrule owner validation
- `aoa-playbooks` coordinates recurring work; it does not become a scheduler
- `aoa-memo` preserves lessons or recovery patterns; it does not become refresh truth

## Adaptation note

Do not import the bundle tree literally.

Adapt it into live repo-native surfaces:

- keep doctrine and boundary language in `Agents-of-Abyss` and `8Dionysus`
- keep typed hint and decision carry in `aoa-sdk`
- keep refresh-law meaning in the owner repos
- keep derived refresh visibility in `aoa-stats`
- keep recurring route meaning in `aoa-playbooks`
- keep optional writeback under existing memo kinds in `aoa-memo`

## Alpha component set

The first bounded component set is:

1. `component:codex-plane:shared-root`
2. `component:skills-export:foundation`
3. `component:codex-subagents:projection`
4. `component:stats-derived-summaries:growth-refinery`

## Honest landing order

1. `Agents-of-Abyss`
2. `8Dionysus`
3. `aoa-sdk`
4. `aoa-skills`
5. `aoa-agents`
6. `aoa-stats`
7. `aoa-playbooks`
8. `aoa-memo`

`Dionysus` keeps the bundle, staging note, and future-lineage route until the
owner slices are actually landed upstream.
