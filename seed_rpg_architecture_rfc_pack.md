seed_id: seed.rpg.architecture-rfc.v0
title: RPG Architecture RFC Pack
profile_anchor: 8Dionysus
projects:
  - AoA
kind: prep-pack-note
status: pending_archive
priority: now
parent_seed: seed.rpg.second-wave-pack.v0
tags:
  - rpg
  - architecture
  - terminology
  - boundary-map
  - dual-vocabulary

# Seed Note: RPG Architecture RFC Pack

## Purpose

Prepare the hard architectural RPG RFC as a named prep pack without opening a fresh numbered wave.

This pack stages the shared vocabulary law, boundary law, dual-vocabulary overlay, and the first runtime/frontend contract surfaces so later bridges, SDK readers, and runtime projections grow from one fixed grammar.

## Why this belongs in Dionysus

`Dionysus` is the seed garden and dispatch layer.

This prep pack belongs here because:

- the rollout spans multiple owning repositories
- it should stay transport-shaped until the owner repos accept the landings
- the existing RPG lineage should remain visible instead of being flattened into a new umbrella wave
- the package is better treated as staging and transport than as a new doctrine authority

## Bundle contents to preserve

The source artifact is `archive/seed_pack_exports/rpg_architecture_rfc_seed.zip`.

It carries:

- `rpg_architecture_rfc_seed/README.md`
- `rpg_architecture_rfc_seed/APPLY_ORDER.md`
- `rpg_architecture_rfc_seed/CODEX_NOTES.md`
- `rpg_architecture_rfc_seed/SEED_MANIFEST.yaml`
- `rpg_architecture_rfc_seed/COVERAGE_MAP.md`
- `rpg_architecture_rfc_seed/TOUCHPOINTS_NOT_INCLUDED.md`
- repo-local architecture-RFC surfaces for `Agents-of-Abyss` and `abyss-stack`
- this `Dionysus` prep-pack note, map, and quest object

## Repo-native landing note

The transport bundle still carries the `abyss-stack` quest as `ABY-Q-0001`.

When planted into the live repo, that quest should be adapted to the repo-native `ABYSS-STACK-Q-*` line rather than reviving the older prefix.

## Ownership posture

Read the later plantings under these repo-home boundaries:

- `Agents-of-Abyss` owns the architecture RFC, canonical terminology, boundary map, and dual-vocabulary overlay
- `abyss-stack` owns the runtime/frontend contract examples and runtime-facing schema posture
- `Dionysus` owns only seed-garden staging and transport lineage

This pack does not make `Dionysus` the owner of RPG doctrine, runtime meaning, or frontend projection law.

## Scope exclusions

This pack explicitly excludes:

- `aoa-sdk`
- `aoa-agents`
- `aoa-evals`
- `aoa-playbooks`
- `aoa-memo`
- `aoa-routing`
- `aoa-skills`
- `aoa-techniques`
- `8Dionysus`
- `Tree-of-Sophia`
- `aoa-kag`
- `ATM10-Agent`

## Suggested planting order

Keep the default order flexible but bias toward:

1. `Agents-of-Abyss`
2. `abyss-stack`
3. `Dionysus`

## Final rule

Land the law before the body.
The honest win here is that later bridges, SDK readers, and runtime projections inherit one cited vocabulary and one cited boundary map instead of improvising them.
