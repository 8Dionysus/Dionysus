# Federated Audit Remediation Wave 3 WS04 First-Slice Planning Packet

Date: `2026-04-07`

## Goal

Land the narrowest owner-first `WS04` slice: make
`aoa-memo/examples/recall_contract.object.working.return.json` a true
`inspect -> capsule -> expand` contract and make `aoa-routing` validate that
return seam explicitly.

## Repositories

- `aoa-memo`
- `aoa-routing`

## Non-goals

- no broad `route_capsule_ref` field migration yet
- no redesign of `return_navigation_hints.min.json` into a larger action graph
- no new return reason classes
- no widening of federation entry kinds
- no `WS03` memory-scope grammar work in `aoa-agents`
- no routing ownership creep into memo-side return semantics

## Validator Of Record

Per repo:

- `aoa-memo`: `python scripts/validate_memo.py`
- `aoa-routing`: `python scripts/validate_router.py --techniques-root /srv/aoa-techniques --skills-root /srv/aoa-skills --evals-root /srv/aoa-evals --memo-root /srv/aoa-memo --agents-root /srv/aoa-agents --aoa-root /srv/Agents-of-Abyss --playbooks-root /srv/aoa-playbooks --kag-root /srv/aoa-kag --tos-root /srv/Tree-of-Sophia`

## Stop Condition

Stop this slice once:

- `recall_contract.object.working.return.json` declares the memo object capsule
  surface explicitly
- `aoa-memo` validators treat the return-ready contract as a real
  inspect/capsule/expand seam instead of a working-only exception
- `aoa-routing` validates the same contract cross-repo before generating return
  navigation hints
- existing memo return navigation in `aoa-routing` stays bounded and
  source-owned without growing a wider return choreography surface

## Rollback Marker

If this slice requires a new return-navigation schema family, a broad
cross-repo field migration, or a router-owned interpretation layer for memo
continuity, stop and roll back to the last state where both repo-native
validators still pass with the narrower return-ready contract.
