# Federated Audit Remediation Wave 4 WS12 GitHub Track Packet

Date: `2026-04-07`

## Goal

Close the final `WS12` GitHub-only track in `ATM10-Agent` by landing one
public product-edge contract that makes release cadence, CI/test tiers,
supported profiles, and the `ATM10-Agent` x `abyss-stack` boundary explicit.

## Repository

- `ATM10-Agent` via GitHub-only issue + PR flow

## Non-goals

- no local checkout execution source for `ATM10-Agent`
- no claim that tagged releases or semantic-version cadence already exist
- no hidden promotion of future `NVIDIA` / `Ollama` or other non-OpenVINO host
  profiles
- no claim that current repo CI proves broad `abyss-stack` parity across Fedora
  or other stack-owned deployment profiles

## Validator Of Record

- GitHub milestone `#1`: `WS12 - Public Product Edge Contract`
- GitHub issue `#49`: `WS12: establish public product-edge contract`
- GitHub PR `#50`: `[codex] Add public product edge posture contract`
- `Pytest` workflow on PR `#50` with `test` and `smoke` jobs both green

## Stop Condition

Stop this track once:

- `ATM10-Agent` publishes one canonical public product-edge contract doc
- `README.md` and `docs/SOURCE_OF_TRUTH.md` route toward that contract
- the issue and milestone are both closed after the landed PR
- `Dionysus` can validate the landed owner-repo anchors through a GitHub-aware
  reality canary without requiring a local `ATM10-Agent` checkout

## Rollback Marker

If the GitHub-only track starts requiring local repo execution, host-specific
test claims beyond the current Windows 11 baseline, or broad `abyss-stack`
parity language without corresponding validation, stop and roll back to the
last state where the public product-edge contract stayed bounded to the landed
owner-repo docs and GitHub-native validation surfaces only.
