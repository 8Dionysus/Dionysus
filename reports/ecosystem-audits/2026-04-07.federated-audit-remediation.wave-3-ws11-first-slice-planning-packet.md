# Federated Audit Remediation Wave 3 WS11 First-Slice Planning Packet

Date: `2026-04-07`

## Goal

Land the narrowest owner-first `WS11` slice: make public onboarding, support,
release, and CI posture explicit across `8Dionysus`, `Agents-of-Abyss`, and
`aoa-sdk` without widening into `ATM10-Agent`, `abyss-stack`, or a new derived
dashboard surface.

## Repositories

- `8Dionysus`
- `Agents-of-Abyss`
- `aoa-sdk`

## Non-goals

- no GitHub-only `WS12` product-edge contract work inside `ATM10-Agent`
- no `abyss-stack` support-matrix or runtime-support rewrite in this slice
- no new center doctrine beyond routing to already-owned surfaces
- no new runtime guarantees or support promises that exceed current validators
- no redesign of `CHARTER.md`, `ECOSYSTEM_MAP.md`, workspace bootstrap, or SDK
  compatibility policy
- no broad public-doc reorganization outside the smallest canonical posture
  notes and README links

## Validator Of Record

Per repo:

- `8Dionysus`: `git diff --check -- README.md docs/PUBLIC_ENTRY_POSTURE.md`
- `Agents-of-Abyss`: `python scripts/validate_ecosystem.py`
- `aoa-sdk`: `python -m pytest -q`

## Stop Condition

Stop this slice once:

- `8Dionysus` routes readers toward one canonical ecosystem-understanding path,
  one canonical local control-plane path, and states its own profile boundary
- `Agents-of-Abyss` publishes one short public support/release/CI posture note
  that stays subordinate to center ownership
- `aoa-sdk` publishes one short release/support/CI posture note tied to actual
  validation commands and workflows
- `WS12` still reads as explicitly deferred GitHub-only product-edge work

## Rollback Marker

If this slice starts requiring new product-edge claims for `ATM10-Agent`,
runtime guarantees for `abyss-stack`, or a broader docs redesign that moves
canonical authority away from the current owner repos, stop and roll back to
the last state where the three owner repos still pass their repo-native
validation batteries with the narrower posture-only change.
