# Federated Audit Remediation Wave 3 WS03 First-Slice Planning Packet

Date: `2026-04-07`

## Goal

Land the narrowest owner-first `WS03` slice: normalize the shared recall-scope
grammar in `aoa-agents`, bind self-agent `memory_scope` back to role-facing
rights, and publish memo-side `scope_classes` in object inspect surfaces
without widening playbook, routing, or distill authority.

## Repositories

- `aoa-agents`
- `aoa-memo`

## Non-goals

- no `aoa-playbooks` scope-family expansion yet
- no new routing-owned interpretation layer for memory scope
- no broad role-law rewrite beyond the shared recall-scope grammar
- no widening of `archivist` / `distill` into hidden write authority
- no `route_capsule_ref` migration or return-corridor redesign
- no Wave 3 proof-lineage, readiness/maturity, or onboarding work

## Validator Of Record

Per repo:

- `aoa-agents`: `AOA_PLAYBOOKS_ROOT=/srv/aoa-playbooks AOA_EVALS_ROOT=/srv/aoa-evals AOA_MEMO_ROOT=/srv/aoa-memo AOA_ROUTING_ROOT=/srv/aoa-routing python scripts/validate_agents.py`
- `aoa-memo`: `python scripts/validate_memo.py`

## Stop Condition

Stop this slice once:

- `aoa-agents` uses one shared recall-scope grammar for
  `memory_rights.allowed_recall_scopes` and self-agent `memory_scope`
- the self-agent checkpoint example is validated against the matched profile's
  recall-scope rights instead of carrying a posture label in the wrong field
- `aoa-memo` object-facing inspect surfaces publish `scope_classes` derived
  from concrete object `scope` identifiers
- memo-side docs name `scope` versus `scope_classes` explicitly and keep the
  object identifiers source-owned

## Rollback Marker

If this slice requires a new playbook-scoping family, a routing-owned scope
interpreter, or a broader role-law rewrite around `archivist` / `distill`,
stop and roll back to the last state where both repo-native validator stacks
still pass with the narrower shared scope grammar only.
