# AoA SDK Skill Runtime Recommendation Gap Fix Session Harvest

Date: `2026-04-06`
Session ref: `session:2026-04-06-aoa-sdk-skill-runtime-gap-fix`
Sanitization posture: public-safe recap; raw terminal output and non-repo host details are intentionally omitted.

## Scope

- reviewed source: bounded `aoa-sdk` runtime-seam remediation session
- owning repository touched: `aoa-sdk`
- adjacent surface validated: `aoa-skills` runtime install contract through canonical `.agents/skills` roots
- validated closeout:
  full `pytest` suite and `ruff` passed in `aoa-sdk`, and live `aoa skills detect` / `aoa skills guard` checks confirmed auto-discovered repo-install availability

## Session result

- closed the recommendation-vs-host-availability gap end-to-end instead of stopping at reporting-only annotation
- taught `aoa-sdk` to auto-discover canonical install roots and keep manual host manifests as explicit overrides
- preserved honest control-plane semantics by demoting router-only skills out of `activate_now`

## Harvest verdict

There is narrow but real harvest here.

The honest survivor is:

1. one reusable control-plane law for splitting routing truth from host actionability and resolving actionability from canonical install roots rather than from repo-export theater

This session does not honestly produce a playbook seed, automation packet, or final quest-promotion verdict.

## Kept candidates

### 1. Recommendation Truth / Host Actionability Split

- unit kind: `law`
- abstraction shape: `technique_candidate`
- owner repo recommendation: `aoa-techniques`
- chosen next artifact: bounded `TECHNIQUE.md` candidate for control-plane availability seams
- nearest wrong target: `aoa-skills`
- why this owner:
  the surviving value is reusable control-plane practice about truth separation, install-root precedence, and actionability semantics
- why not the nearest wrong target:
  this is not one bounded leaf workflow to invoke; it is implementation law for routers, detectors, and reports

## Explicit deferrals

- `aoa-automation-opportunity-scan`
  Reason: one bounded seam fix is not enough reviewed repeat signal to justify an automation-readiness packet.
- `aoa-quest-harvest`
  Reason: no single repeated reviewed quest-shaped unit remains as the only unresolved object.

## Public-safe evidence anchors

- [2026-04-06.aoa-sdk.skill-runtime-recommendation-gap-fix-session-harvest.packet.json](/srv/Dionysus/reports/ecosystem-audits/2026-04-06.aoa-sdk.skill-runtime-recommendation-gap-fix-session-harvest.packet.json)
- key `aoa-sdk` surfaces:
  [skill-runtime-recommendation-gap.md](/srv/aoa-sdk/docs/skill-runtime-recommendation-gap.md),
  [skill-runtime-recommendation-gap-fix-spec.md](/srv/aoa-sdk/docs/skill-runtime-recommendation-gap-fix-spec.md),
  [detector.py](/srv/aoa-sdk/src/aoa_sdk/skills/detector.py),
  [main.py](/srv/aoa-sdk/src/aoa_sdk/cli/main.py)
- closeout proof summary:
  full `aoa-sdk` `pytest` suite and `ruff` green, plus live repo-install auto-discovery confirmed through `skills detect` and `skills guard`

## Final read

This session produced one real technique candidate about control-plane
actionability truth.

It did not honestly produce a new skill, a playbook seed, an automation packet,
or a quest-promotion verdict.
