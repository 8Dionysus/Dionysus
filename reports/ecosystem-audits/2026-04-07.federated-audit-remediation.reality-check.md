# Federated Audit Remediation Reality Check

Date: `2026-04-07`

## Scope

This follow-up reality check compares the staged remediation pack against the
current owner-repo surfaces after the first planted slices in:

- `Agents-of-Abyss`
- `aoa-sdk`
- `aoa-evals`
- `aoa-skills`
- `Tree-of-Sophia`
- `abyss-stack`
- `aoa-routing`
- `aoa-playbooks`
- `aoa-kag`
- `aoa-agents`
- `aoa-memo`
- `8Dionysus`
- `Dionysus`

Context carried forward:

- the preferred `abyss-stack` source checkout remains `~/src/abyss-stack`
- `ATM10-Agent` remains a GitHub-only deferred endcap for this program

## Landed First Slices

### `WS01`

`Agents-of-Abyss` still keeps compact registry v1 bounded, but it no longer
pretends `aoa-sdk` is outside machine-readable federation coverage entirely.
`generated/federation_supporting_inventory.min.json` now carries `aoa-sdk` as a
supporting consumer surface, and center docs route readers to that split
honestly.

### `WS02`

`aoa-sdk` now treats route-directed public surfaces as compatibility-gated only:
`src/aoa_sdk/routing/picker.py` no longer falls back to raw path loads, and the
first latest-sibling canary lane exists in
`.github/workflows/latest-sibling-canary.yml` with an explicit sibling matrix in
`scripts/sibling_canary_matrix.json`.

### `WS10`

`WS10` is no longer only seeded in `aoa-sdk`. `aoa-evals` now carries its own
latest-sibling canary lane in `.github/workflows/latest-sibling-canary.yml`
backed by `scripts/run_sibling_canary.py` and
`scripts/sibling_canary_matrix.json`. That canary proves the current eval proof
surface against latest `aoa-techniques`, `aoa-skills`, `aoa-agents`,
`aoa-playbooks`, `aoa-memo`, `aoa-stats`, and a real `abyss-stack` source
checkout, not a `/srv` runtime mirror.

`Agents-of-Abyss` and `Tree-of-Sophia` now add the next `WS10` layer from the
source side rather than the consumer side. Each repository carries its own
scheduled `source-side-smoke.yml` workflow that runs the current owner-native
validation battery on an unsynchronized schedule. The center and the source
tree now have distinct smoke towers, so mesh expansion no longer depends only
on consumer-facing repos hearing drift first.

`abyss-stack` now adds the next runtime-side `WS10` layer with a dedicated
mirror canary in `.github/workflows/mirror-canary.yml`. That lane materializes
a temporary deployed `Configs` tree from the source checkout, runs
`scripts/aoa-sync-configs --delete`, and then proves
`python scripts/validate_stack.py --parity-check` against the materialized
runtime mirror. This keeps the stack-side source/deployed boundary observable
without treating `/srv/abyss-stack` as an editable source repo.

The remaining `WS10` tail is now closed in `aoa-routing`, `aoa-playbooks`, and
`aoa-kag`. Those repositories already carried compatibility canaries, but they
were synchronized onto the same exact schedule. Their canary lanes are now
desynchronized, and `aoa-routing` was refreshed to current sibling state so the
mesh no longer depends on a known-stale router snapshot. In practice this means
the remaining routing, playbook, and KAG towers can hear drift at different
times instead of failing in one correlated pulse.

### `WS05`

`WS05` no longer stays fully deferred behind the broad old ToS/KAG framing.
The trimmed follow-through is now landed across the three owner repos that
actually own the seam:

- `Tree-of-Sophia` now has a public `python scripts/validate_tiny_entry_route.py`
  surface, and the current docs and workflows now state the route in clearer
  vocabulary: `README.md` stays the public `tos-root`, the worked tiny-entry
  example stays the public compatibility authority surface, and source-first
  re-entry is stated explicitly before any derived `kag_view` or adjunct.
- `aoa-routing` now keeps `tos-root` return navigation bounded in a stronger
  way: the federation root return still restores `Tree-of-Sophia/CHARTER.md` as
  root authority, but it now also publishes one source-owned secondary re-entry
  back to `Tree-of-Sophia/examples/tos_tiny_entry_route.example.json` instead of
  making the ToS return posture end at abstract root authority.
- `aoa-kag` now lands the second trimmed follow-through slice without widening
  the seam. The standalone `AOA-K-0011` ToS adjunct and the matching
  `federation_spine` entry both carry machine-checkable `adjunct_budget` and
  `subordinate_posture` fields, which makes the adjunct explicitly opt-in,
  bounded to one route family, forbidden from becoming the numbered tiny path,
  and source-first by pointing consumers back to
  `Tree-of-Sophia/examples/tos_tiny_entry_route.example.json`.

This keeps the seam source-first without replaying the whole older workstream
and closes the trimmed `WS05` track instead of leaving `aoa-kag` as an open
adjunct-budget tail.

### `WS06`

`Dionysus` now has a durable remediation ledger, Wave 0 / Wave 1 planning
packets, and a machine-checkable owner-repo reality canary tied to the current
first-slice anchors.

### `WS03`

Wave 3 is now no longer only about the return corridor. The first narrow
`WS03` slice is also landed across the two owner repos that actually own the
memory-scope contract:

- `aoa-agents` now treats `memory_rights.allowed_recall_scopes` as a shared
  recall-scope grammar instead of a coarse repo/project/ecosystem shortcut.
  The self-agent checkpoint surface no longer overloads `memory_scope` with a
  posture label like `bounded_recall`; the schema and example now keep
  `memory_scope` inside the same role-facing recall-scope classes, and the
  validator binds that field back to the matched profile's
  `allowed_recall_scopes`.
- `aoa-memo` still keeps concrete object `scope` identifiers such as
  `thread:...` or `repo:...`, but the object-facing inspect surfaces now also
  publish derived `scope_classes`. That gives agent-layer consumers a typed
  memo-side handoff field they can compare against the shared scope grammar
  without parsing object-local identifiers or pretending `aoa-agents` owns memo
  object shape.

This is intentionally narrower than the broader old `WS03` wording. The slice
does not widen `aoa-playbooks`, does not redesign role law beyond the shared
scope grammar, and does not turn `archivist/distill` into hidden write
authority.

### `WS04`

Wave 3 is no longer only planned. The first narrow `WS04` slice is now landed
across the two owner repos that actually hold the current return seam:

- `aoa-memo` now treats
  `examples/recall_contract.object.working.return.json` as a real
  `inspect -> capsule -> expand` contract rather than a working-only exception.
  The return-ready contract now declares
  `generated/memory_object_capsules.json` explicitly, local validators enforce
  that shape, and the routing-adoption doc now states the same object-family
  join rule for checkpoint continuity.
- `aoa-routing` does not widen `return_navigation_hints.min.json`, but it now
  cross-validates the owner-side memo return contract before accepting the memo
  return seam as current. In practice that means routing must see the same
  working return contract, inspect surface, capsule surface, expand surface,
  and checkpoint continuity posture that `aoa-memo` itself publishes.

This is intentionally narrower than the broader old `WS04` wording. The
`route_capsule_ref` field split is still deferred, but the live return corridor
is now typed and validator-backed enough to stop treating `working.return` as a
special uncapsuled exception.

### `WS07`

Wave 3 now also has a first landed proof-lineage and governance-surface slice
across the two owner repos that actually own those meanings:

- `aoa-skills` still keeps source-owned skill maturity in `status`, but the
  public governance surface now also spells that out as `maturity_status` and
  separates it from derived `default_reference_readiness`. That derived field
  now reads `ready`, `blocked`, or `not_applicable`, so project overlays with
  no governance lane stop looking silently blocked while remaining outside the
  default-reference cohort. The old `candidate_ready` cohort survives only as a
  compatibility alias for downstream consumers.
- `aoa-evals` already had full proof-lineage in the heavy catalog, but the thin
  reader surfaces now expose compact `technique_refs`, `skill_refs`,
  `evidence_kinds`, and `proof_surface_kinds` in both
  `generated/eval_catalog.min.json` and `generated/eval_capsules.json`. That
  makes routing and local inspection proof-lineage-aware without widening the
  bounded claim wording inside bundle-local `EVAL.md`.

This is intentionally narrower than the broader old `WS07` wording. The slice
does not redesign `generated/governance_backlog.*`, does not promote any skill,
does not change eval claim doctrine, and does not widen into `WS11`
onboarding/support/release work.

### `WS11`

Wave 3 now also lands the first public onboarding/support/release slice across
the three owner surfaces that actually hold those claims:

- `8Dionysus` now makes its profile boundary explicit in
  `docs/PUBLIC_ENTRY_POSTURE.md`. The profile routes readers toward one
  canonical ecosystem-understanding path in `Agents-of-Abyss`, one canonical
  local control-plane path in `aoa-sdk`, and states that product-edge release
  and test-tier tightening still belongs to the later GitHub-only `WS12`
  track.
- `Agents-of-Abyss` now keeps a compact
  `docs/PUBLIC_SUPPORT_POSTURE.md` surface that says what the center may
  publicly support, which files must align before a center claim is honest, and
  which verification tiers reinforce those center claims.
- `aoa-sdk` now keeps a compact `docs/RELEASE_CI_POSTURE.md` surface that says
  what an SDK release may honestly claim and maps those claims onto three real
  verification tiers: local control-plane checks, release reinforcement, and
  scheduled sibling-drift detection.

This is intentionally narrower than the broader old `WS11` wording. The slice
does not rewrite `ATM10-Agent`, does not claim product-edge support guarantees,
does not widen into `abyss-stack`, and does not build a new cross-repo dashboard
for public status.

### `WS12`

Wave 4 is now no longer deferred. The GitHub-only `WS12` track is landed in
`ATM10-Agent` through one bounded public product-edge contract:

- GitHub milestone `#1` (`WS12 - Public Product Edge Contract`) and issue
  `#49` were opened as the explicit product-edge tracking lane, then closed
  after the work landed.
- `ATM10-Agent` PR `#50` (`[codex] Add public product edge posture contract`)
  merged at commit `a7ae973fb12004484919d31961875dad7fd49a02`.
- `ATM10-Agent` now publishes `docs/PRODUCT_EDGE_POSTURE.md` on `main` as the
  canonical public contract for release cadence, CI/test tiers, supported
  profiles, and the explicit `ATM10-Agent` x `abyss-stack` boundary.
- `README.md` and `docs/SOURCE_OF_TRUTH.md` now route to that canonical
  product-edge contract instead of leaving those claims scattered.
- The landed PR carried live GitHub validation through the existing `Pytest`
  workflow, with both `test` and `smoke` jobs green on the PR run before
  merge.

This closes the GitHub-only endcap without inventing a local checkout or
pretending that tagged releases, future host profiles, or broad
`abyss-stack` parity already exist. The track stays honest by making those
limits explicit in the landed owner-repo docs.

### `WS08` and `WS09`

`aoa-skills` repo validation now runs:

- `python scripts/validate_skills.py --fail-on-review-truth-sync`
- `python scripts/report_technique_drift.py --techniques-repo ../aoa-techniques --fail-on-drift`
- `python scripts/release_check.py --include-packaging-smoke`

The portable export workflow also makes `skills-ref` availability blocking with
`--require-skills-ref`.

## Still Open

- none

## Gate B Read

Wave 2 now holds on the currently planted scope:

- every major cluster touched in Wave 2 has at least one canary or smoke path
- the core towers no longer share one synchronized canary pulse
- trimmed `WS05` is fully landed as follow-through rather than expanding into a
  broader ToS/KAG redesign
- `abyss-stack` checks drift from the preferred source checkout instead of
  claiming owner meaning from `/srv`

## Gate C Read

Wave 3 now holds on the currently planted scope:

- `WS03` normalized shared memory-scope grammar and posture-critical scope
  classes without prose parsing
- `WS04` made the live `working.return` corridor validator-backed across
  `aoa-memo` and `aoa-routing`
- `WS07` separated source-owned maturity from derived readiness and exposed
  compact proof-lineage in thin eval reader surfaces
- `WS11` now makes public onboarding/support/release posture match the current
  owner-repo verification surfaces in `8Dionysus`, `Agents-of-Abyss`, and
  `aoa-sdk`

## Current Read

The staged remediation pack is no longer partially landed.
It now acts as a landed lineage surface with the full remediation program
closed through bounded owner-repo landings, including the GitHub-only `WS12`
endcap in `ATM10-Agent`.
There is no remaining workstream in this remediation pack. Any future work now
belongs to new owner-repo changes or new seed surfaces, not to replaying this
pack as an active queue.
