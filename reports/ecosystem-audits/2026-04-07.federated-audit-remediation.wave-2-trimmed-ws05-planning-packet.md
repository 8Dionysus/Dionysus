# Federated Audit Remediation Wave 2 Trimmed WS05 Planning Packet

Date: `2026-04-07`

## Goal

Land only the still-open ToS/routing sub-slice of `WS05`: make the current
`tos-root` re-entry source-first and validator-backed without replaying the
older broad ToS/KAG redesign.

## Repositories

- `Tree-of-Sophia`
- `aoa-routing`

## Non-goals

- no new federation starter for the current ToS tiny-entry route
- no widening of active federation entry kinds
- no graph-first or multi-hop ToS entry redesign
- no direct `aoa-kag` adjunct jump as the primary ToS re-entry
- no `aoa-kag` adjunct-budget second slice yet
- no Wave 3 memory, return-corridor, proof-lineage, or onboarding work

## Validator Of Record

Per repo:

- `Tree-of-Sophia`: `python scripts/validate_tiny_entry_route.py`
- `aoa-routing`: `python scripts/validate_router.py --techniques-root /srv/aoa-techniques --skills-root /srv/aoa-skills --evals-root /srv/aoa-evals --memo-root /srv/aoa-memo --agents-root /srv/aoa-agents --aoa-root /srv/Agents-of-Abyss --playbooks-root /srv/aoa-playbooks --kag-root /srv/aoa-kag --tos-root /srv/Tree-of-Sophia`

## Stop Condition

Stop this trimmed slice once:

- `Tree-of-Sophia` owns a public validator for the current tiny-entry route
- ToS docs and workflows name the public compatibility authority surface and
  source-first re-entry explicitly
- `aoa-routing` publishes one bounded `tos-root` secondary re-entry back into
  the source-owned tiny-entry example
- routing still does not jump directly to the `aoa-kag` adjunct as primary ToS
  re-entry

## Rollback Marker

If the slice requires a new federation kind, a new starter, or a broader ToS /
KAG ABI change, stop and roll back to the last state where both owner-repo
validators still pass without widening the workstream.
