# Federated Audit Remediation Wave 1 Planning Packet

Date: `2026-04-07`

## Goal

Remove the clearest current lies and false greens without widening scope past
the first coherent slices.

## Repositories

- `Agents-of-Abyss`
- `aoa-sdk`
- `aoa-skills`
- `Dionysus`

## Non-goals

- no `aoa-routing` or `aoa-playbooks` semantic redesign yet
- no `aoa-agents` / `aoa-memo` memory-grammar changes yet
- no `Tree-of-Sophia` / `aoa-kag` trimmed Wave 2 follow-through yet
- no `ATM10-Agent` Wave 4 GitHub track yet
- no `~/src/abyss-stack` mirror canary until source contracts stabilize further

## Validator Of Record

Per repo:

- `Agents-of-Abyss`: `python scripts/validate_ecosystem.py`
- `aoa-sdk`: `python -m pytest -q`
- `aoa-skills`: `python scripts/validate_skills.py --fail-on-review-truth-sync`
- `Dionysus`: `python scripts/validate_seed_surfaces.py`

## Stop Condition

Stop Wave 1 once:

- `aoa-sdk` is machine-visible through a supporting inventory surface
- routed SDK reads cannot bypass compatibility rules
- `aoa-sdk` has one scheduled latest-sibling canary lane
- `aoa-skills` CI can fail on review truth drift, technique drift, and packaging smoke
- `Dionysus` canary and lifecycle markers agree with the live first-slice state

## Rollback Marker

If any repo-local change widens beyond its declared first slice, roll back to
the last state where the repo's validator of record still passes without
needing a broader wave.
