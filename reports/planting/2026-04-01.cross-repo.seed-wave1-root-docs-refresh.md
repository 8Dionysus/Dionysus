# Planting Report

## Metadata

- Date: 2026-04-01
- Wave: none (bounded cross-repo root docs refresh)
- Seed: `seed.wave1.root-docs-refresh.v0`
- Source ref: `archive/seed_pack_exports/seed_wave1_root_docs_refresh_2026-04-02.zip`
- Target repo: `cross-repo: 8Dionysus, Agents-of-Abyss, Dionysus, abyss-stack`
- Planting status: landed
- PR / commit / issue: local workspace planting trace; no single target-repo PR recorded here yet
- Maintainer: Codex

## Contract being planted

This planting applies one bounded root-entry refresh across the first-wave public
repositories without rewriting deeper doctrine. The root `README.md` files are
shortened and made more route-first, while the root `AGENTS.md` files become
clearer about boundaries, working order, and verification. The owning
repositories keep their meaning; `Dionysus` only preserves the bundle, the
transplant map, and the durable trace.

## Landed surfaces

- Human-readable surface: root `README.md` in `8Dionysus`, `Agents-of-Abyss`, `Dionysus`, and `abyss-stack`
- Structural surface: root `AGENTS.md` in `8Dionysus`, `Agents-of-Abyss`, `Dionysus`, and `abyss-stack`
- Validation surface: `python scripts/validate_seed_surfaces.py`, `python scripts/validate_ecosystem.py`, and `python scripts/validate_stack.py`

## Structural artifact

`seed_wave1_root_docs_refresh.map.yaml` is the structural artifact that keeps
the repo order, scope limits, guardrails, and source bundle replayable from
inside `Dionysus`.

## Validation

- Command run: `python scripts/validate_seed_surfaces.py`
- Result: passed
- Manual review notes: `Dionysus` stayed aligned after bundle intake, prep-pack linkage, root refresh, and report creation

- Command run: `python scripts/validate_ecosystem.py`
- Result: passed
- Manual review notes: root-only center rewrite should not absorb layer-owned meaning or break route docs

- Command run: `python scripts/validate_stack.py`
- Result: passed
- Manual review notes: one small route fix was needed so the shorter root README still points to `docs/BRANCH_POLICY.md` and `docs/RECURRENCE_RUNTIME_POLICY.md`; validation was run against the source checkout in `~/src/abyss-stack`

- Command run: local markdown-link resolver across the refreshed root `README.md` and `AGENTS.md` files
- Result: passed
- Manual review notes: all local markdown links in the refreshed roots resolve from their owning repository roots

## Deferred zones / non-goals

- no deep doc rewrites outside root `README.md` and root `AGENTS.md`
- no wave manifest changes
- no `seed_index` promotion
- no new public/private status claims
- no ownership transfer away from the existing repo homes

## Risks / follow-ups

- `Agents-of-Abyss` and `abyss-stack` intentionally lose a large amount of root-level exposition, so any future deep-route expansion should live in linked docs rather than reflating the root.
- If `aoa-sdk` should become more visible beyond root routing, align deeper center or profile docs in a separate bounded pass instead of widening this refresh.
- If `abyss-stack` keeps growing operational depth, a later `docs/START_HERE.md` or index-style page may be worth adding so the root can stay compact.

## Vocabulary preserved

- route-first
- constitutional center
- seed garden
- public foyer
- runtime substrate
- source-of-truth order
- boundary-explicit
