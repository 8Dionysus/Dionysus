# Planting Report

## Metadata

- Date: 2026-04-11
- Wave: none (future prep pack closeout)
- Seed: `seed.aoa.growth-refinery-docket-pack.v0` + `seed.aoa.growth-refinery-contract-seed-pack.v0`
- Source ref: `archive/seed_pack_exports/aoa-growth-refinery-docket-pack-2026-04-11.zip`; `archive/seed_pack_exports/aoa-growth-refinery-contract-seed-pack-2026-04-11.zip`
- Target repo: `cross-repo: Agents-of-Abyss, aoa-sdk, aoa-skills, Dionysus, aoa-stats, aoa-evals, aoa-playbooks, aoa-memo`
- Planting status: landed
- PR / commit / issue:
  - `Agents-of-Abyss` PR #56 merged as `d1f40802f4bd24ee9876baaf3d15a11d93478d1f`
  - `aoa-sdk` PR #53 merged as `0d3f0b4141422d374f3b7a49c238546e62af4965`
  - `aoa-skills` PR #159 merged as `e2bba10f9f3136d9888d3c928d28db85798d7b62`
  - `Dionysus` staging PR #68 merged as `72890dcf97046afc08c7583cc32964396ed08b0e`
  - `aoa-stats` PR #20 merged as `baeedadde723eafb84652be9fdb859eac9926f30`
  - `aoa-evals` PR #92 merged as `3850da18badb4ae66e30fa98c0eac9d8e181870d`
  - `aoa-playbooks` PR #90 merged as `4559150326b765d1b3a3d047c7648e8f7c2ccf88`
  - `aoa-memo` PR #71 merged as `bd4c944c2dcc600776f558457f40a2f5a851e37f`
- Dionysus lifecycle cleanup: updated both linked prep-pack markers from staged-only to `landed_upstream_retained_for_lineage` and moved them into the landed lineage bucket in `docs/SEED_SURFACE_MAP.md`
- Maintainer: Codex

## Contract being planted

This closeout records that the growth-refinery docket route and its contract
companion are now fully landed slice by slice in the owner repos, while
`Dionysus` returns to lineage-only custody.

The docket pack remains the durable record of route order, owner boundaries,
and stop-lines. The contract pack remains the durable record of the v1 lineage
substrate, examples, acceptance gates, and tiny validator posture, but owner
truth now lives in the landed repos rather than in the transport packs.

The preserved axis stays `cluster_ref -> candidate_ref -> seed_ref ->
object_ref`, with `aoa-sdk` limited to carry, `aoa-skills` limited to
reviewed candidate minting, `Dionysus` limited to seed identity and planting
trace, `aoa-stats` derived-only, `aoa-evals` proof-only, `aoa-playbooks`
recurring-route only, and `aoa-memo` subordinate writeback only.

One landed example chain now runs from
`aoa-sdk/examples/checkpoint_lineage_hint.example.json` through
`aoa-skills/examples/session_growth_artifacts/harvest_packet.alpha.json`,
`Dionysus/examples/seed_lineage_entry.example.json`,
`aoa-stats/examples/candidate_lineage_summary.example.json`, the three
lineage-aware eval bundles, `aoa-playbooks/playbooks/session-growth-cycle/PLAYBOOK.md`,
and the lineage-aware memo examples in `aoa-memo`. Dropped and superseded
branches remain representable through `nearest_wrong_target`, `supersedes`,
`merged_into`, and `drop_reason`.

## Landed surfaces

- Human-readable surface: `docs/REVIEWABLE_GROWTH_REFINERY.md` in `Agents-of-Abyss`; `docs/CANDIDATE_LINEAGE_CARRY.md` in `aoa-sdk`; `docs/GROWTH_REFINERY_WRITEBACK.md` in `aoa-memo`
- Structural surface: `schemas/checkpoint_lineage_hint.schema.json` in `aoa-sdk`; `schemas/seed_lineage_entry.schema.json` in `Dionysus`; `schemas/candidate_lineage_summary.schema.json` in `aoa-stats`
- Validation surface: `python scripts/validate_seed_lineage_examples.py` in `Dionysus`; `python scripts/validate_repo.py` in `aoa-stats`; `python scripts/validate_repo.py` in `aoa-evals`; `python scripts/validate_playbooks.py` in `aoa-playbooks`

## Structural artifact

`schemas/seed_lineage_entry.schema.json` is the structural artifact because it
is the owner-local bridge where reviewed `candidate_ref` becomes
`Dionysus`-owned `seed_ref` without letting `Dionysus` steal owner truth from
upstream layers.

## Validation

- Command run: `cd /srv/Dionysus && python scripts/validate_seed_surfaces.py`
- Result: passed
- Manual review notes: checked both growth-refinery prep-pack notes, the landed bucket move in `docs/SEED_SURFACE_MAP.md`, and this report against the merged owner trail

- Command run: `cd /srv/Dionysus && python scripts/build_seed_route_map.py --check`
- Result: passed
- Manual review notes: the route map stayed aligned after the lifecycle cleanup

- Command run: `cd /srv/Dionysus && python scripts/validate_seed_route_map.py`
- Result: passed
- Manual review notes: route references remained intact after the closeout edits

- Command run: `cd /srv/Dionysus && python -m pytest -q tests`
- Result: passed
- Manual review notes: repo-local validators, including seed-lineage example checks, stayed green

- Command run: `cd /srv/aoa-stats && python scripts/validate_repo.py`
- Result: passed
- Manual review notes: `candidate_lineage_summary` still builds from reviewed receipts rather than raw checkpoint notes

- Command run: `cd /srv/aoa-evals && python scripts/validate_repo.py`
- Result: passed
- Manual review notes: lineage integrity, owner-fit, and repair-boundedness bundles remain present with explicit claim limits and blind spots

- Command run: `cd /srv/aoa-playbooks && AOA_AGENTS_ROOT=/srv/aoa-agents AOA_EVALS_ROOT=/srv/aoa-evals AOA_SKILLS_ROOT=/srv/aoa-skills AOA_MEMO_ROOT=/srv/aoa-memo python scripts/validate_playbooks.py`
- Result: passed
- Manual review notes: `session-growth-cycle` resolves its eval anchors and required skills against the merged workspace surfaces

## Deferred zones / non-goals

- no new numbered wave or `navigation.next_live_seed` change
- no first-authoring move into `aoa-routing`, `aoa-kag`, or `/srv/.codex`
- no claim that `Dionysus` is now the owner of candidate, proof, playbook, or memory truth
- no maturity/status upgrade beyond preserving the landed route as lineage evidence

## Risks / follow-ups

- Treat these packs as replayable lineage only from here on out; any next
  growth-refinery work should start from the live owner repos, not from the
  archived bundle trees.
- If a later campaign widens the chain beyond the current
  `cluster_ref -> candidate_ref -> seed_ref -> object_ref` axis, capture that
  as new owner-repo truth first and only then revisit Dionysus transport
  markers.
- Keep the tiny validator posture bounded to structural consistency. Do not let
  it grow into a second sovereign contract layer inside `Dionysus`.

## Vocabulary preserved

- `cluster_ref`
- `candidate_ref`
- `seed_ref`
- `object_ref`
- reviewed donor harvest
- nearest wrong target
- acceptance gates
- derived-only
- proof-only
- bounded writeback
