# Planting Report

## Metadata

- Date: 2026-04-01
- Wave: none (bounded cross-repo root docs refresh)
- Seed: `seed.wave2.root-docs-refresh.v0`
- Source ref: `archive/seed_pack_exports/seed_wave2_root_docs_refresh_2026-04-02.zip`
- Target repo: `cross-repo: Tree-of-Sophia, aoa-kag, aoa-memo, aoa-playbooks, aoa-routing`
- Planting status: landed
- PR / commit / issue: `Tree-of-Sophia` pushed to `main` as `a71578d`; `aoa-kag` PR `#39` merged as `b73723c`; `aoa-memo` PR `#37` merged as `cd678fa`; `aoa-playbooks` PR `#45` merged as `7d2b2df`; `aoa-routing` PR `#51` merged as `991ba89`; `Dionysus` pack staged and pushed as `17e9593`
- Maintainer: Codex

## Contract being planted

This planting applies one bounded root-entry refresh across the second-wave ToS
and AoA layer repositories without rewriting deeper doctrine. The root
`README.md` files are shortened and made more route-first, while the root
`AGENTS.md` files become clearer about boundaries, working order, and
validation. The owning repositories keep their meaning; `Dionysus` only
preserves the bundle, the transplant map, and the durable trace.

## Landed surfaces

- Human-readable surface: root `README.md` in `Tree-of-Sophia`, `aoa-kag`, `aoa-memo`, `aoa-playbooks`, and `aoa-routing`
- Structural surface: root `AGENTS.md` in `Tree-of-Sophia`, `aoa-kag`, `aoa-memo`, `aoa-playbooks`, and `aoa-routing`
- Validation surface: `python scripts/validate_seed_surfaces.py`, `python scripts/validate_kag_export.py`, `python scripts/release_check.py`, `python scripts/validate_memo.py`, `python scripts/validate_playbooks.py`, and `python scripts/validate_router.py`

## Structural artifact

`seed_wave2_root_docs_refresh.map.yaml` is the structural artifact that keeps
the repo order, scope limits, guardrails, and source bundle replayable from
inside `Dionysus`.

## Validation

- Command run: `python scripts/validate_seed_surfaces.py`
- Result: passed
- Manual review notes: `Dionysus` stayed aligned after bundle intake, prep-pack linkage, report hydration, and root README visibility updates

- Command run: `python scripts/validate_kag_export.py`
- Result: passed
- Manual review notes: `Tree-of-Sophia` root tightening kept source/intake/tree/export posture intact without breaking the bounded export seam

- Command run: `python scripts/release_check.py`
- Result: passed
- Manual review notes: `aoa-kag` root refresh stayed docs-only; generated substrate outputs remained current and reviewable after the pass

- Command run: `python scripts/validate_memo.py`
- Result: passed
- Manual review notes: `aoa-memo` root refresh preserved memory-class, provenance, and temporal boundary language while the validator stayed green

- Command run: `python scripts/validate_playbooks.py`
- Result: passed
- Manual review notes: `aoa-playbooks` root refresh preserved authored-bundle, composition, and evidence posture without widening scope

- Command run: `python scripts/validate_router.py`
- Result: passed
- Manual review notes: `aoa-routing` root refresh kept thin-router boundaries explicit and left generated routing outputs valid

- Command run: local markdown-link resolver across the refreshed root `README.md` and `AGENTS.md` files
- Result: passed
- Manual review notes: all local markdown links in the refreshed roots resolve from their owning repository roots

## Deferred zones / non-goals

- no deep doc rewrites outside root `README.md` and root `AGENTS.md`
- no wave manifest changes
- no `seed_index` promotion
- no new public/private status claims
- no ownership transfer away from the existing repo homes

## Risks / follow-ups

- `Tree-of-Sophia` intentionally tightens a very large public entry, so follow-up route growth should continue in linked docs rather than reflating the root.
- `aoa-kag`, `aoa-memo`, `aoa-playbooks`, and `aoa-routing` now depend even more on grouped route families staying accurate as deeper docs evolve.
- If any root validator exposes a newly required adjacent sync, keep it explicit and smaller than the root refresh itself.

## Vocabulary preserved

- route-first
- source-first
- derived substrate
- memory is not proof
- playbook is not a skill
- thin routing
- boundary-explicit
