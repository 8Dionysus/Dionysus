# langgraph-sidecar-llamacpp-v1

This folder keeps the first failing `llama.cpp` fixture-gate attempt visible as
a reviewable mirror.

It is retained on purpose and was later superseded by
`langgraph-sidecar-llamacpp-promotion-gate/`.

Machine-readable runtime truth stays local under `/srv/abyss-stack/Logs/local-ai-trials/`.
